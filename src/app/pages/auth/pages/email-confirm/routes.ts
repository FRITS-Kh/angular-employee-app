import { Routes } from '@angular/router';

import { EmailConfirmComponent } from './email-confirm.component';

export const EMAIL_CONFIRM_ROUTES: Routes = [
  {
    path: '',
    component: EmailConfirmComponent,
  },
];
